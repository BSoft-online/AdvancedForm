import { styled } from '@material-ui/styles';

export const Label = styled('label')({
    fontStyle: 'normal',
    fontWeight: 600,
    lineHeight: 2,
    display: 'block',
    marginBottom: '1em',
});
